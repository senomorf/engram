name: codeql

# Static security analysis (code scanning). Kotlin needs a real compile to
# extract, so we build explicitly (manual mode) like the ci job does; the runner
# already provides the Android SDK.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "27 3 * * 1" # weekly, catches advisories that land after merge

permissions:
  contents: read
  security-events: write

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v6
      - uses: github/codeql-action/init@v3
        with:
          languages: java-kotlin
          build-mode: manual
      # daemon-less + in-process Kotlin so the compile runs in the traced process;
      # otherwise the Gradle/Kotlin daemon hides it from CodeQL and nothing extracts
      - run: >
          ./gradlew :app:assembleDebug :cli:assemble
          --no-daemon -Dkotlin.compiler.execution.strategy=in-process --console=plain
      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java-kotlin"
